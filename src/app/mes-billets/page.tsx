import { PageShell } from '@/components/page-shell'

export const metadata = { title: 'Mes billets — Fodium' }

export default function MesBilletsPage() {
  return (
    <PageShell
      title="Mes billets"
      lead="Vos billets et vos navettes, prêts à être scannés."
    />
  )
}
